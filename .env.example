# Public — exposed to browser
NEXT_PUBLIC_SUPABASE_URL=https://bbelawsdhsjdayqsagih.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_iSlUg8d6T0v4CMfGu_if8g_0HFJM4Fr

# Server-only — bypasses RLS for trusted writes (admin updates, atomic order creation)
# Get from: Supabase dashboard → Project settings → API → service_role secret
SUPABASE_SERVICE_ROLE_KEY=

# Admin gate
ADMIN_PASSWORD=vinea-admin-2026
# Long random string used to sign the admin cookie (separate from password)
ADMIN_SECRET=replace-with-a-long-random-string
